import styled from "styled-components";

export const SortComponentStyle = styled.div`
  display: flex;
  justify-content: flex-start;
  align-items: center;

  gap: 1rem;

  #sort,
  #cardNumber {
    outline: none;
    border: none;
    font-size: 1rem;
    padding: 1rem;
    box-shadow: rgba(0, 0, 0, 0.16) 0px 1px 4px;
  }
`;
